import React from 'react';
import Navigation from '../navigation/Navigation';

const Header = () => {
  return (
    <header>
      <h1>Tech Blog</h1>
      <Navigation />
    </header>
  );
};

export default Header;
